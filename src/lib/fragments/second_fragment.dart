import 'package:flutter/material.dart';

class SecondFragment extends StatelessWidget {
  const SecondFragment({super.key});

  @override
  Widget build(BuildContext context) {
    // ignore: todo
    // TODO: implement build
    return const Center(
      child: Text("Hello Fragment 2"),
    );
  }
}
